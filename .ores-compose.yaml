schema_version: ores.compose.v1
project: des-mcp-server
allow_lazy_start: false

services:
  des-mcp-server:
    name: des-mcp-server
    runtime: host
    build:
      - ["cargo", "build", "--bins"]
    command: ["./target/debug/des-mcp-server.rs"]
